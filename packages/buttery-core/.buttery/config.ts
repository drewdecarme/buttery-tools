import type { ButteryConfig } from "../src/config";

const config: ButteryConfig = {
  commands: {
    name: "test",
    description:
      "Testing out the internal reconciliation process of the buttery config",
  },
};
export default config;
